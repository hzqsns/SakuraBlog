{
  "name": "sakura-backend",
  "version": "1.0.0",
  "description": "Sakura Blog Backend API with Go + Gin + PostgreSQL",
  "scripts": {
    "start": "cd ../backend && go run ./cmd/server",
    "build": "cd ../backend && go build -o bin/server ./cmd/server",
    "dev": "cd ../backend && go run ./cmd/server"
  },
  "keywords": ["blog", "api", "go", "gin", "postgresql"],
  "author": "",
  "license": "MIT"
}
