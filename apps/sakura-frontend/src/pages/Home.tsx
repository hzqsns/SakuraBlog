import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function Home() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>博客内容区域</CardTitle>
      </CardHeader>
      <CardContent>
        <p>主要内容区域暂时留空</p>
      </CardContent>
    </Card>
  )
} 